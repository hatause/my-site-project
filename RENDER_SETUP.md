# 🚀 Быстрая настройка Neon PostgreSQL на Render

## ⚡ Быстрые шаги

### 1. Создайте базу данных в Neon (2 минуты)

1. Зайдите на https://neon.tech
2. Создайте проект
3. Скопируйте **Connection String** (выглядит как: `postgresql://user:pass@host/db?sslmode=require`)

### 2. Добавьте переменные окружения на Render (1 минута)

В вашем Render сервисе → **Environment** → добавьте:

```
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
JWT_SECRET=ваш-секретный-ключ-минимум-32-символа
PORT=10000
NODE_ENV=production
```

⚠️ **Важно**: Замените `DATABASE_URL` на ваш Connection String из Neon!

### 3. Обновите код (уже сделано!)

✅ `package.json` - добавлен `pg` вместо `sqlite3`
✅ `server.js` - переписан для PostgreSQL

### 4. Деплой на Render

1. Закоммитьте изменения:
   ```bash
   git add .
   git commit -m "Migrate to PostgreSQL (Neon)"
   git push
   ```

2. Render автоматически задеплоит изменения

3. Таблицы создадутся автоматически при первом запуске

### 5. Проверка

1. Откройте **Logs** в Render
2. Должно быть: `✅ Подключение к PostgreSQL успешно установлено`
3. Должно быть: `✅ Таблицы базы данных инициализированы`

---

## 🔍 Проверка подключения

Если видите ошибки в логах:

### Ошибка: "Connection refused"
→ Проверьте, что Connection String правильный

### Ошибка: "SSL required"  
→ Убедитесь, что в Connection String есть `?sslmode=require`

### Ошибка: "Table does not exist"
→ Подождите несколько секунд - таблицы создаются автоматически

---

## 📝 Формат Connection String

Должен выглядеть так:
```
postgresql://username:password@ep-xxx-xxx.region.aws.neon.tech/dbname?sslmode=require
```

---

## ✅ Готово!

Ваш сайт теперь использует Neon PostgreSQL! 🎉

Подробная инструкция: см. `NEON_SETUP_GUIDE.md`

