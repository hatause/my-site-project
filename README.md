# PC Optimizer - Сайт оптимизации компьютеров

Современный веб-сайт, посвященный оптимизации компьютеров, с системой регистрации пользователей и отзывами.

## Особенности

- 🎨 Современный минималистичный дизайн
- ✨ Плавные анимации и переходы
- 📱 Адаптивный дизайн для всех устройств
- 🔐 Система аутентификации пользователей
- 💬 Раздел отзывов с возможностью оставлять и просматривать отзывы
- 👤 Страница "О себе" с информацией о разработчике
- 🛡️ Безопасная обработка пользовательских данных

## Технологии

### Backend
- Node.js
- Express.js
- SQLite (база данных)
- JWT (аутентификация)
- bcryptjs (хэширование паролей)

### Frontend
- HTML5
- CSS3 (с анимациями)
- Vanilla JavaScript
- Responsive Design

## Установка и запуск

### Требования
- Node.js (версия 14 или выше)
- npm (Node Package Manager)

### Шаги установки

1. **Клонируйте репозиторий или перейдите в директорию проекта**

2. **Установите зависимости для backend:**
   ```bash
   cd backend
   npm install
   ```

3. **Настройте переменные окружения:**
   Создайте файл `.env` в директории `backend` на основе `.env.example`:
   ```bash
   cp .env.example .env
   ```
   
   Отредактируйте `.env` и установите свои значения:
   ```
   PORT=3000
   JWT_SECRET=your-super-secret-key-change-this-in-production
   ```

4. **Запустите backend сервер:**
   ```bash
   npm start
   ```
   
   Или для разработки с автоматической перезагрузкой:
   ```bash
   npm run dev
   ```
   
   Сервер запустится на `http://localhost:3000`

5. **Откройте сайт в браузере:**
   - После запуска backend сервера, откройте браузер и перейдите по адресу: `http://localhost:3000`
   - Backend сервер автоматически обслуживает frontend файлы
   - Вы увидите главную страницу сайта

## Структура проекта

```
site/
├── backend/
│   ├── server.js          # Основной файл сервера
│   ├── package.json       # Зависимости backend
│   ├── .env.example       # Пример файла окружения
│   └── .gitignore         # Игнорируемые файлы
├── frontend/
│   ├── index.html         # Главная страница
│   ├── reviews.html       # Страница отзывов
│   ├── about.html         # Страница "О себе"
│   ├── styles.css         # Стили
│   ├── auth.js            # Аутентификация
│   ├── reviews.js         # Логика отзывов
│   └── main.js            # Основной JavaScript
└── README.md              # Документация
```

## API Endpoints

### Аутентификация
- `POST /api/register` - Регистрация нового пользователя
- `POST /api/login` - Вход пользователя
- `GET /api/me` - Получить информацию о текущем пользователе (требует токен)

### Отзывы
- `GET /api/reviews` - Получить все отзывы
- `POST /api/reviews` - Создать новый отзыв (требует токен)

## Использование

1. **Регистрация:** Нажмите кнопку "Регистрация" и заполните форму
2. **Вход:** Используйте кнопку "Вход" для авторизации
3. **Отзывы:** После входа вы можете оставлять отзывы на странице "Отзывы"
4. **О себе:** Просмотрите информацию о разработчике на странице "О себе"

## Безопасность

- Пароли хэшируются с использованием bcryptjs
- JWT токены используются для аутентификации
- Валидация данных на стороне сервера
- Защита от SQL-инъекций через параметризованные запросы

## Разработчик

**Демьян** - 18-летний начинающий программист

## Лицензия

MIT

